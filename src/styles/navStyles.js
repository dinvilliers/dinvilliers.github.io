import styled, { css } from "styled-components"
import { motion } from 'framer-motion'

export const Nav = styled.div`
	position: fixed;
	top: 0;
	left: 0;
	width: 100%;
	height: 100%;
	display: block:
	colour: black;
	z-index: 100;
`